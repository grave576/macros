#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define out(a) cout<<a<<endl
#define min(a, b) (((a) < (b)) ? (a) : (b))
#define max(a, b) (((a) > (b)) ? (a) : (b))
#define get(n) int n;scanf("%d", &n)
#define get2(a,b) cin>>a>>b
#define get3(a,b,c) scanf("%d%d%d", &a, &b, &c)
#define clr(a,x) memset(a,x,sizeof(a))
#define pb push_back
#define F(n) for(int i=0;i<n;i++)
#define FR(m,n,z) for(int i=m;i<n;i+=z)


int main() {
	int t;
	cin>>t;
	
	while(t--)
	{
	    int n;
	    cin>>n;
	    vector <int> v;
	    for(int i=0;i<n;i++) 
	    {
	        int d;
	        cin>>d;v.push_back(d);
	    }
	}
	
	return 0;
}
